import { proxy } from "@/lib/proxy";
export const dynamic = "force-dynamic";
export const GET = proxy,
  POST = proxy,
  PUT = proxy,
  PATCH = proxy,
  DELETE = proxy,
  HEAD = proxy,
  OPTIONS = proxy;
